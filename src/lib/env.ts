// import { parseEnv } from "znv";
// import { z } from "zod";

// export const { PAT,GH_CLIENT_ID,GH_SECRET } = parseEnv(import.meta.env, {
//   PAT: z.string().min(1),
//   GH_SECRET: z.string().min(1),
//   GH_CLIENT_ID: z.string().min(1),
// });

// // export const pb_url = import.meta.env.PAT
// // export const pb_auth_collection = import.meta.env.RAKKAS_PB_AUTH_COLLECTION
